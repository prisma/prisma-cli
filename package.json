{
  "name": "prisma-cli",
  "version": "8.0.0-rc.1",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@11.6.0",
  "scripts": {
    "build": "turbo run build",
    "build:cli": "turbo run build --filter=@prisma/cli",
    "build:compute": "pnpm --filter @prisma/compute build",
    "format": "biome format . --write",
    "lint": "biome check . --error-on-warnings",
    "lint:fix": "biome check . --write",
    "smoke:cli-nextjs": "node scripts/smoke-cli-nextjs-artifact.mjs",
    "bump-version": "node scripts/bump-version.ts",
    "test": "turbo run test",
    "test:scripts": "node --test scripts/determine-version-utils.test.ts scripts/set-version-utils.test.ts scripts/resolve-package-version.test.mjs",
    "typecheck": "turbo run typecheck",
    "prisma-cli": "tsx packages/cli/src/bin.ts",
    "prisma": "tsx packages/cli/src/bin.ts",
    "prisma-v8": "tsx packages/cli/src/v8/bin.ts",
    "check:conformance": "turbo run conformance"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.16",
    "pkg-pr-new": "^0.0.75",
    "tsx": "^4.22.4",
    "turbo": "^2.10.9"
  }
}
