name: Auto-repin product CLI packages

# When composer or prisma/prisma publish a family package, this repo
# repins automatically and the merged repin ships as a dev version of
# the CLI (operator ruling 2026-08-13; docs/oss/versioning.md). Only a
# real CLI release needs a human.
#
# Triggers:
#   - repository_dispatch `family-published`: sent by the product
#     repos' publish workflows the moment they ship.
#   - schedule (daily): the backstop for a missed dispatch — the same
#     comparison against the registry, so a lost event delays a repin
#     by at most a day.
#   - workflow_dispatch: run it by hand.
#
# The PR is opened with REPIN_PAT, not GITHUB_TOKEN, for two reasons:
# pull requests created with the workflow token never trigger CI, and
# without CI the required checks never report, so auto-merge would hang
# forever. The PAT is the wmadden-electric bot's.

on:
  repository_dispatch:
    types: [family-published]
  schedule:
    - cron: "17 6 * * *"
  workflow_dispatch:

concurrency:
  group: auto-repin
  cancel-in-progress: false

jobs:
  repin:
    name: Repin against the registry
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false

      - name: Set up pnpm
        uses: pnpm/action-setup@0e279bb959325dab635dd2c09392533439d90093 # v6.0.8

      - name: Set up Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version-file: .node-version

      - name: Compare pins against the registry
        id: repin
        run: node scripts/auto-repin.mjs

      # `pnpm install --no-frozen-lockfile` scoped to the lockfile
      # refresh: the repin edits one manifest, and the PR must carry the
      # matching lockfile or every CI job fails on --frozen-lockfile.
      - name: Refresh the lockfile
        if: ${{ steps.repin.outputs.changed == 'true' }}
        run: |
          corepack enable
          pnpm install --lockfile-only --no-frozen-lockfile

      - name: Open the repin PR with auto-merge
        if: ${{ steps.repin.outputs.changed == 'true' }}
        env:
          GH_TOKEN: ${{ secrets.REPIN_PAT }}
          SUMMARY: ${{ steps.repin.outputs.summary }}
        run: |
          if [ -z "$GH_TOKEN" ]; then
            echo "::error::REPIN_PAT is not configured; cannot open a CI-triggering PR. Add the bot PAT as the REPIN_PAT repository secret."
            exit 1
          fi
          BRANCH="auto/repin-$(date -u +%Y%m%d%H%M%S)"
          git config user.name "willbot"
          git config user.email "w.a.madden+machine@gmail.com"
          git checkout -b "$BRANCH"
          git add packages/cli/package.json pnpm-lock.yaml
          git commit -s --trailer "Signed-off-by: Will Madden <madden@prisma.io>" \
            -m "Repin product CLI packages to the registry: $SUMMARY" \
            -m "Opened automatically (auto-repin.yml); merging ships a dev version of the CLI. A real release stays a human act."
          git push "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" "$BRANCH"
          PR_URL=$(gh pr create \
            --title "Repin product CLI packages: $SUMMARY" \
            --body "Automated repin from the registry ($SUMMARY). Merging publishes a \`dev\` version of the CLI automatically; the conformance checks on this PR are what stand between a family publish and that dev build. Opened by auto-repin.yml." \
            --base main --head "$BRANCH")
          gh pr merge --auto --squash "$PR_URL"
          echo "Opened $PR_URL with auto-merge armed."
